mern-pdf-generator
│
├── backend
│   ├── controllers
│   │   └── userController.ts     # Handles user login, registration
│   │   └── productController.ts  # Handles product data, PDF generation
│   ├── models
│   │   └── User.ts               # MongoDB schema for user data
│   │   └── Invoice.ts            # MongoDB schema for invoice data
│   ├── routes
│   │   └── userRoutes.ts         # API routes for user authentication
│   │   └── productRoutes.ts      # API routes for product and PDF generation
│   ├── utils
│   │   └── jwt.ts                # JWT token handling utilities
│   ├── app.ts                    # Express app setup
│   ├── server.ts                 # Main server file
│   ├── package.json              # Dependencies for backend
│   ├── tsconfig.json             # TypeScript configuration for backend
│   └── .env                      # Environment variables for backend (e.g., DB URI)
│
├── frontend
│   ├── src
│   │   ├── components
│   │   │   └── Login.tsx         # Login component
│   │   │   └── Register.tsx      # Registration component
│   │   │   └── AddProduct.tsx    # Add product form component
│   │   │   └── GeneratePDF.tsx   # PDF generation and download component
│   │   ├── redux
│   │   │   └── store.ts          # Redux store setup
│   │   │   └── userSlice.ts      # User state management
│   │   │   └── productSlice.ts   # Product state management
│   │   ├── services
│   │   │   └── api.ts            # Axios instance for API calls
│   │   ├── App.tsx               # Main React App component
│   │   ├── main.tsx              # Entry point for Vite build
│   ├── tailwind.config.js        # Tailwind CSS configuration
│   ├── vite.config.ts            # Vite configuration
│   ├── tsconfig.json             # TypeScript configuration for frontend
│   ├── package.json              # Dependencies for frontend
│   └── .env                      # Environment variables for frontend (API URLs)
│
├── README.md                     # Instructions on how to set up and run the project
└── .gitignore                    # Files and folders to be ignored by Git
